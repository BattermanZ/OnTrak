# Server Configuration
# -----------------
NODE_ENV=development  # Options: development, production

# MongoDB Configuration
# --------------------
MONGODB_URI=mongodb://localhost:27017/ontrak  # Local MongoDB connection string

# Security
# --------
JWT_SECRET=your-secret-key-here  # Change this to a strong random string in production

# Logging Configuration
# -------------------
LOG_LEVEL=debug  # Options: error, warn, info, debug (use debug for development, info for production)

# Note: The following values have been hardcoded in the application:
# - PORT=3456 (in app.js)
# - Frontend URL=http://localhost:3000 (in CORS configuration)
# - Socket.io URL=http://localhost:3456 (in frontend socket hook)
# - API URL=http://localhost:3456/api (in frontend API service)

# JWT Configuration
JWT_EXPIRES_IN=1d

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000 